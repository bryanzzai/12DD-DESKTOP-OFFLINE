# macOS offline edition

This branch is the macOS target for the shared offline web application.

`The12DayDancer.app/Contents/Info.plist` records the known macOS bundle identity. The application wrapper runs the same local offline web content as the Windows edition.

Large browser media files are delivered in the offline package, not committed to Git.
